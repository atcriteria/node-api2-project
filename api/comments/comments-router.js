const Comment = require('./comments'); // these are my helper functions
const express = require('express'); // loading in express to help create router

const router = express.Router();

// Create Comments endpoints below
router.get('/', (req, res) => {

});

module.exports = router;